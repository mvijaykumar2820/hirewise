# Render uses this file to detect how to build and run the service
# See https://render.com/docs/deploy-fastapi

# Build Command
pip install -r requirements.txt

# Start Command  
uvicorn main:app --host 0.0.0.0 --port $PORT
